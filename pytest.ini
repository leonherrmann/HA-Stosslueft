[pytest]
testpaths = tests
# `python -m pytest` puts the working directory on sys.path but the `pytest`
# script does not, so without this the tests only import custom_components when
# they happen to be started the first way.
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
